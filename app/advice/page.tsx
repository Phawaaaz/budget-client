/*
 * THESIS: Advice is read off the shape of your own months. Category small multiples on one month axis replace the category-default feed of tip cards.
 * OWN-WORLD: Folio's graphite on near-white, hairline rules, green in / coral out, category hues on bars, dashed ink budget lines; guides live on the yellow planner field.
 * STORY: Fawaz sees what grew, what held steady and what repeats, reads why it matters, then jumps to the transactions or budget behind it.
 * FIRST VIEWPORT: Heading and range control, then money in / out / kept as three month-bar multiples across the full width.
 * FORM: Category small multiples, candidate 3 of 7; seed 06188ca6.
 * FINISH: unreviewed and undocumented is unfinished; this build ends with the finish review, the verdict, and DESIGN.md
 */
import MoneyWorkspace from "../components/money-workspace";

export const metadata = { title: "Advice | Folio" };

export default function AdvicePage() {
  return <MoneyWorkspace initialView="Advice" />;
}
